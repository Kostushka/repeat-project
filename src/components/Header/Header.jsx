import styles from './Header.module.css';

const Header = () => {
    return (
        <>
            <header className={styles.header}>Заголовок</header>
        </>
    );
};

export default Header;
