import MainInfo from './MainInfo';
import PostContainer from './PostContainer/PostContainer';

const Main = () => {
    return (
        <main>
            <MainInfo />
            <PostContainer />
        </main>
    );
};

export default Main;
